export const gameProps = {
  JET_SPEED: 600, // pixels per seconds
  ROTATION_LERP_FACTOR: 30.0, // smooth rotation
  CONTENT_MIN_GAP: 400, // Minimum distance from player to content
  CONTENT_MAX_GAP: 500, // Maximum distance from player to content
  JET_SIZE: 4, // Multiplier for jet sprite size (e.g., 4 = 20px sprite scaled to 80px)
  CLOUD_SIZE: 1, // Multiplier for cloud sprite size (e.g., 1 = 10px sprite scaled to 10px)
  MAX_ZOOM: 2.0, // Maximum zoom level
  MIN_ZOOM: 0.5, // Minimum zoom level
  ZOOM_STEP: 0.1, // Zoom increment step
}